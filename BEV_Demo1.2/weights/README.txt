Put the two checkpoints from your v14 notebooks here:
  mobilenet_lfab_model.pth      (Notebook 2 output, Model A)
  efficientnet_lfab_model.pth   (Notebook 3 output, Model B)
They must come from the v14 (LFAB v4) runs. Older checkpoints have different LFAB keys and will not load.
